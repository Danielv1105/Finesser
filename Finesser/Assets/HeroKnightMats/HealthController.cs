using System.Collections;
using System.Collections.Generic;
using Unity.VisualScripting;
using UnityEngine;
using UnityEngine.Analytics;
using UnityEngine.UI;

public class HealthController : MonoBehaviour
{

    // Deklaration der Spielerlebens
    //public int playerHealth;

    // Spieler starter mit 3 Leben
    //void Start()
    //{
    //    playerHealth = 3;
    //}

    // Methode, um Schaden nehmen zu können -> In der UI gibt es zwei verschiedene Objekte
    // für ein Herz (ein Leeres und ein Volles in der Ebene darüber) ->
    // Wenn die Methode nun aufgerufen wird, dann verschwindet das volle Herz in der UI und das leere bleibt vorhanden
    // bzw. es wird auf "nicht aktiv" gesetzt. Übrig bleibt in der UI dann nur noch das leere und die verbleinden vollen Herzen
    // außerdem wird die Anzahl an Leben (im Wert der Variable) verringert -> Bsp. durch Fallen oder Gegner
    
    
}
